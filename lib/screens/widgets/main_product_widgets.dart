import 'package:flutter/material.dart';

class MainProductWidget extends StatelessWidget {
  const MainProductWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}